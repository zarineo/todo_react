import Input from "../Input/Input";
import Select from "../Select/Select";

const Form = () => {
  return (
    <form className="todo__form">
      <Input />
      <Select />
    </form>
  );
};

export default Form;
